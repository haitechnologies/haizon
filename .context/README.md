# .context/ — LLM/AI Agent Context Directory

Read in order:
1. `rules.md` — What you can and cannot do
2. `architecture.md` — System structure
3. `conventions.md` — Naming and patterns

For DB details: `src/Core/DB.php` (table constants), `docs/DATABASE.md` (catalog).
